#include <stdio.h>

int main() {

    int N, cc = 0;
    scanf("%d", &N);
    int letra;

    while (cc < N) {
        scanf("%d", &letra);
        printf("%d\n", letra);
        cc++;
    }
    // Jesus alguem me ajuda
}
